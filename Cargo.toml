[package]
name = "alpress"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "alpress"
path = "src/main.rs"

[dependencies]
flate2 = "1.0"      # Gzip — Phase 3
lz4    = "1.24"     # LZ4  — Phase 4 (speed-focused)
zstd   = "0.13"     # Zstd — Phase 4 (best ratio/speed balance)